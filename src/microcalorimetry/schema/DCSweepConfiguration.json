{
    "title": "Configuration of a DC Sweep",
    "type": "object",
    "properties": {
        "record_settings": {
            "type": "object",
            "properties": {
                "maxlen": {
                    "type": "integer",
                    "description": "maximum length of the data record in memory"
                },
                "minlen": {
                    "type": "integer",
                    "description": "minimum length of the data record in memory"
                }
            },
            "additionalProperties": false
        },
        "first_sample_delay": {
            "type": "number",
            "description": "How long to wait after adjusting source before taking first sample (seconds).",
            "additionalProperties": false
        },
        "off_duration": {
            "type": "number",
            "description": "Duration of off period between settings. Set to 0 to turn off.",
            "additionalProperties": false
        },
        "step_duration": {
            "type": "number",
            "description": "How long to hold each setting in seconds.",
            "additionalProperties": false
        },
        "instruments": {
            "type": "object",
            "additionalProperties": {
                "$ref": "InstrumentRoles.json#/definitions/InstrumentRole",
                "description": "Additional properties should all satisfy the definition of an instrument role."
            }
        }
    },
    "required": ["record_settings","first_sample_delay","off_duration","step_duration","instruments"]
}